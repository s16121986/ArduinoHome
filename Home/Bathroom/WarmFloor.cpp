namespace Bathroom{ namespace WarmFloor{
	/*ACDimmer dimmer1;
	unsigned int offDelay;

	void command(byte action, byte data = 0) {
		switch (action) {
			case C_ON:
			case C_PWM:
				dimmer1.on(data);
				state.set(STATE_ON, data);
				break;
			case C_OFF:
				dimmer1.off();
				state ^= STATE_ON;
				break;
		}
	}
	void dimmerAttachFn() { triacHigh(PIN_BT_WARMFLOOR, offDelay); }
	void setup() {
		dimmer1.init(PIN_BT_WARMFLOOR, PIN_BT_WARMFLOOR_A, dimmerAttachFn, offDelay);
	}*/
	void setup() {

	}
	void listen() {
		//s1.listen();
		//s2.listen();
	}
	void loop() {

	}
} }
